/** Sub-merchant group layout. The parent (merchant) layout provides the sidebar shell. */
export default function SubMerchantLayout({ children }: { children: React.ReactNode }) {
  return children;
}
