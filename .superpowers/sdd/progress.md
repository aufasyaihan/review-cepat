# SDD Progress — feat/device (device claim & reseller flow)
Task 1: resolveSetupToken — complete (commit 1f94c1a..038abf1, review clean, spec ✅ approved; minor: KEY redeclared in describe block — inert, defer to final review)
Task 2: resolvePostClaim — complete (commit 038abf1..7c87011, review clean, spec ✅ approved; minor: test doesn't verify .set() payload, defer to final review)
Task 3: ownerForgetDevice — complete (commit 7c87011..b3f3ebb, review clean, spec ✅ approved; minor: tests shallow on .set payload + org-mismatch path, brief-mandated, defer to final review)
Task 4: setupClaimCodeAction branch — complete (commit b3f3ebb..3aabbb6, review clean, spec ✅, no issues)
Task 5: register org auto-creation — complete (commit 3aabbb6..3566dd4, review clean, spec ✅ approved; minors: assert insert includes phone, phone max/trim untested, non-transactional org-create + slug-loop edge (brief-mandated, known ceiling), defer to final review)
Task 6: ?d= login/register handoff — complete (commit 3566dd4..16a99ff, review clean, spec ✅ approved; minors: resolvePostClaim-throw->fail branch untested, report stat discrepancy cosmetic, defer to final review)
Task 7: /option page — complete (commit 16a99ff..4be80b8 + fix round f7acbfe, re-review approved, spec ✅; minors: swallowed redirect from requireRole inside action try/catch, resell toast claim-code unasserted, page untested — defer to final review)
Task 8: linktree — complete (commit f7acbfe..9e9e6de, review clean, spec ✅ approved; minors: INACTIVE branch untested, fieldset semantic sliver (biome-suggested), defer to final review)
Task 9: dashboard pruning — complete (commit 9e9e6de..2ff6394, review clean, spec ✅ approved; minors: LINK_ROWS<->PERMISSION_ROWS path-completeness unasserted, Forgot entry f-owner visibility untested, defer to final review)
Task 10: delete superseded dashboard routes — complete (commit 2ff6394..d4b42c7, review clean, spec ✅; minors: /api/device/claim/route.ts left standing though its UI/permission row are gone, defer to final review)
Task 11: e2e owner/reseller round trip — complete ({commits}, 2/2 e2e ✅ GREEN, 586 unit tests ✅, review clean; deviations: (a) seed code E2ECLAIM2→E2ECLAIM (8-char OTP truncation), (b) option-actions claim/resell server fixes beyond brief scope — orgless device on /option was unclaimable/resellable by design, (c) api-factories.test.ts stale 'destination' dim→'referrer' to unblock next build, removed pre-existing unused callAndCapture, (d) serialized spec justified in report. Concern: existing setup-claim/google-review/register-claim specs RED against fresh build — 9-10 char seed codes truncated by 8-slot OTP form, pre-existing, untriaged)
Task 11: e2e owner/reseller round trip — complete (commit d4b42c7..378c35d incl. 2 in-scope fix commits 5a3923f/1988891, + fast-follow cbef160; review approved after fixes; spec ✅; minors deferred)
  NOTE Task 11 review IMPORTANT (deferred vector, user-approved minimal scope): /option page mints a free setup token for any visitor (page.tsx:16) and the session gate does not prove claim-code possession. With 1988891, any signed-in MERCHANT owner who knows an org-less device's slug can now claim it into their org, or resell it (adminReset → wipes config, rotates code). Fast-follow cbef160 gates the page on organizationId===null (closes org-bound cases) but the FRESH org-less UNCLAIMED hijack remains open. Design question for final review: require a valid signed token at /option (spec:67 security note); touches setupClaimCodeAction + login/register ?d= handoff + e2e. Also: claimForSelfAction still never sets boundUserId (option-actions.ts:31-39) diverging from spec §4; report's "register-claim also truncated" claim is WRONG (register uses plain Input) — only setup-claim success + google-review are RED from the 8-slot OTP truncation.
  Also: pre-existing e2e specs setup-claim.spec.ts (success test) + google-review.spec.ts are RED on a fresh build because 9-10 char seed codes (E2ECLAIM1 etc.) are truncated to 8 chars by the OTP form; register-claim.spec.ts is unaffected. Recommended sweep: shorten remaining seed codes to 8 chars.
Task 12: final verification — complete (commit cbef160..53e5e6b incl. fix d9d2467, review pending whole-branch; brief Step 1-4 evidence in task-12-report.md: coverage 587 pass / tsc exit 0 / biome baseline-only / playwright 43 pass 7 pre-existing-fail)
  NOTE cbef160 (Task 11 fast-follow) introduced a type+runtime regression: DeviceDetail did not expose organizationId → option/page.tsx gate always redirected to /dashboard. Caught by Task 12 tsc; fixed by d9d2467 (map organizationId ?? null in 3 DeviceDetail sites + fixtures). Lesson: run tsc + e2e after page-gate changes.
  Seed sweep done (53e5e6b): e2e-unclaimed code E2ECLAIM1 -> E2EUNCL1; setup-claim + google-review specs updated. Confirmed needed (OTP 8-slot truncation).
  7 e2e failures at HEAD are pre-existing (reproduced at base 9787869) — NOT feature regressions; list in task-12-report.md.
Final whole-branch review (range 9787869..53e5e6b, review-9787869..53e5e6b.diff): VERDICT YES-WITH-DEFERRED-DECISIONS — mergeable if controller signs off:
  (A) /option code-proof no-op (page self-mints free token; ?d= handoff then binds member into org-less device too) — merge-safe AS-ACCEPTED residual risk per reviewer, fix = require code-minted ?t= at /option, stop self-minting. Controller chose minimal-now-flag (Task 11). CARRY FORWARD.
  (B) claimForSelfAction omitted boundUserId (spec §4) — response isn't a critical blocker but the mechanism turning (A)'s code leak into org compromise. FIXED NOW in eebe04f (sets boundUserId: userId; requireOwnerMembership returns {membership,userId}; set-payload assertion added).
  Orphaned member-assignment: assignDeviceAction/unassignDeviceAction/getMemberById/assignDevice/unassignDevice + /api/member/assign|unassign rows have NO app consumers after Task 10 deleted /user-management/[memberId]. Unplanned product regression (plan §7 silent). DECISION REQUIRED: delete dead exports+rows+tests or restore UI.
  Dead /api/device/claim route + claimWithCodeAction left behind (permission row deleted Task 9; all callers ForbiddenError). CARRY FORWARD (delete both).
  Plus 10 deferred minors + 7 pre-existing e2e failures (reproduced at base) + carry-forward list in final review report (see reviewer output; 7 items). Reviewer: NO Critical findings.
Final fixes (controller-signed-off after YES-WITH-DEFERRED-DECISIONS): (A) token-proof + (B) orphaned-member deletion.
  (A) User decided via question: BLOCK merge until /option requires a code-validated token. Implemented (had subagent been self-blocked by the no-commit, dropped it; I re-owned): setupClaimCodeAction mints setup token BEFORE claimAccountless-branch, passes it as 3rd arg to resolvePostClaim → owner URL /s/slug/option?t=<token>; linkDeviceAfterAuthAction forwards its already-validated ?d= token as the same 3rd arg; option/page.tsx drops the self-mint, requires searchParams.t where resolveSetupToken(t)===device.id else redirect /s/slug/setup; anonymous-with-valid-token → /login?d=<token>. e2e device-option-flow rewritten: both tests enter via /setup with E2ECLAIM, serial mode (shared device + code rotation). COMMIT 2921752.
  (B) Orphaned member-assignment deleted per controller: assignDeviceAction/unassignDeviceAction (+imports), getMemberById/assignDevice/unassignDevice, /api/member/assign|unassign rows (PERMISSION + LINK). Tests updated. listMembers/createUserAction/etc. kept; setDestinationsAction untouched. COMMIT 0d2969d.
  Verification (working tree + both commits): vitest 569/569 (587 - 18 removed w/ assign/getMemberById tests); tsc --noEmit exit 0; biome only CRLF-format baseline (no new lint); full e2e `npm run test:e2e`: 41 passed / 9 failed = documented 7 pre-existing + 2 environmental (:32 admin members, :100 admin-inv copy) — BOTH ALSO FAIL AT HEAD eebe04f (verified via stash, same 6-failure set incl. flaky :127 that passed in full run, failed serial/HEAD runs) → NOT a regression. device-option-flow 2/2 ✓, setup-claim 3/3 ✓, google-review ✓.
  Lesson: `npx playwright test` does NOT build; serve only after `tsx scripts/e2e-server.ts --build-only` (I ran it cold against a stale 05:23 build from the vanished subagent and chased phantom failures; fresh build = 2/2). "destination stream closed early" server logs during action-then-`router.replace` are benign Next stream aborts, not failures.
