# Implementation Plan: NFC QR Redirect Platform — Project Foundation

**Branch**: `001-nfc-qr-redirect` | **Date**: 2026-09-13 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `/specs/001-nfc-qr-redirect/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command; its definition describes the execution workflow.

**Canonical architecture**: `docs/architecture/NFC_PLATFORM_ARCHITECTURE_v3.md` (referenced in the command input as `docs/architecture/ARCHITECTURE.md`; the actual file is `NFC_PLATFORM_ARCHITECTURE_v3.md`). All rules in it are binding.

## Summary

The MVP is a fullstack redirect SaaS: a Next.js 16 App Router monolith where an admin
creates physical NFC/QR devices with unique identities and claim codes, a merchant
registers, claims, and configures single- or multi-link destinations (including Google
review destinations resolved via Google Places), and a public scan of a device either
302-redirects to a single destination or shows a multi-link landing page — with every
scan recorded before the customer proceeds and surfaced in merchant analytics.

Technical approach: Domain-Based Modular Monolith on Next.js 16.3.5 (Node.js runtime),
six capability domains under `domains/`, `app/` strictly for routing and composition,
Drizzle ORM + MySQL, Better Auth + RBAC, TanStack Query SSR prefetch +
HydrationBoundary, TanStack Form + Zod, shadcn/ui + Base UI + Phantom UI, Pino logging,
node:test + Playwright at ≥90% coverage, GitHub Actions CI, and an SSH (pm2) deploy
script for Hostinger.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode) on Node.js 22 LTS runtime,
Next.js 16.3.5 (App Router, `export const runtime = 'nodejs'` on route handlers/segments).

**Primary Dependencies**:

- Next.js 16 (App Router) — fullstack server components + route handlers
- Drizzle ORM (mysql2 driver) + drizzle-kit (generate/migrate workflow)
- Better Auth (Drizzle adapter, MySQL) + `@better-auth/drizzle-adapter`
- TanStack Query v5 — SSR prefetch + HydrationBoundary + useSuspenseQuery
- TanStack Form + Zod — type-safe forms validated at every input boundary
- shadcn/ui (design system) + Base UI (advanced primitives) + Phantom UI (layout only)
- Pino — sole logging library (server-side structured logs)

**Storage**: MySQL 8.x single database; Drizzle schema in `db/`, versioned SQL
migrations generated by drizzle-kit into `drizzle/`, seed script in `db/seed.ts`.

**Testing**: `node:test` (unit) + React Testing Library (component) + MSW (API mocking)
+ Playwright (e2e) + c8/Node coverage; minimum 90% overall coverage; ran in CI.

**Target Platform**: Web application; mobile-first; deployed to Hostinger via SSH
deploy script (`scripts/deploy.sh`) running the app under pm2 as `nfc-platform`.
Constituent App Router pages are SEO-friendly server components.

**Project Type**: web-service (fullstack SaaS web application — no native apps).

**Performance Goals**: scan → destination under 2 seconds on a standard mobile
connection (SC-001); landing and app pages render without visible blocking on mobile;
redirect path avoids any client-side hop.

**Constraints**: WCAG AA; every request produces a structured Pino log; errors logged
with context; no component/hook calls `fetch()` directly (all via domain API); every
page prefetches TanStack Query; page/layout/server-component conventions per
architecture doc rules 1–9; Zod validation at every input boundary; secrets only via
environment variables; ≥90% coverage gate in CI; Biome lint+format gate in CI.

**Scale/Scope**: MVP. Likely merchants in the hundreds, devices in the low thousands,
scan volume event-driven (bursts on physical placement); single MySQL instance with
per-device and daily aggregation queries. No billing, no native apps, no
multi-region hosting. Purchases/claim-code distribution are offline (Assumptions in
spec).

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **I. Architecture First** — PASS. Business capabilities map to exactly one domain each
  (`domains/{auth,merchant,device,destination,scan,analytics}`); `app/` holds routing and
  composition only; business logic lives in `domains/*/server/` never in `page.tsx`/`layout.tsx`.
- **II. Frontend Principles** — PASS. Every `page.tsx` is a Server Component; layouts are
  synchronous and fetch nothing; every page prefetches TanStack Query and hydrates via
  `HydrationBoundary`; client components read data only through `useSuspenseQuery`
  (architecture rule 6). Public landing page is SEO-rendered server component; single-link
  scan path redirects server-side before response.
- **III. API Principles** — PASS. Networking only through `domains/*/api/client.ts +
  queries + mutations`; no `fetch()` in components or hooks; the only route handlers
  (`app/api/auth/[...all]/route.ts`, public scan resolution) translate HTTP into domain
  services.
- **IV. Quality Standards** — PASS. Every feature includes unit tests, ≥90% coverage,
  Playwright e2e for critical flows (claim-config-scan), error/loading/empty states
  (architecture rule 7–8; spec FR series).
- **V. Testing Requirements** — PASS. node:test, RTL, Playwright, MSW, and 90% coverage
  threshold match exactly.
- **VI. Observability** — PASS. Pino only; every request logged with structured context
  (request id, method, path, status, durationMs, actor, device); errors logged with context.
- **VII. Security** — PASS. Better Auth (mandated), RBAC via `domains/*/server/permissions.ts`
  with admin/merchant roles, secrets via env only, Zod validation at every input boundary.
- **VIII. Code Quality** — PASS. TypeScript strict, Biome for lint+format, CI must pass
  before merge.
- **IX. Scalability** — PASS. Each domain exposes a thin API layer and route handlers;
  any domain can later be extracted into its own Next.js route-service or backend service
  without touching the frontend, because UI consumes only the domain API.

No gate violations. No complexity justification required.

## Project Structure

### Documentation (this feature)

```text
specs/001-nfc-qr-redirect/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)
```

### Source Code (repository root)

```text
app/                                # routing + composition only (no business logic)
├── page.tsx                        # marketing homepage (branding, SEO-ready)
├── robots.ts                       # robots directives for public pages
├── sitemap.ts                      # sitemap for public pages
├── (auth)/                         # login + register
│   ├── login/page.tsx
│   └── register/page.tsx
├── (admin)/                        # admin area
│   ├── layout.tsx                  # sync, no fetch
│   ├── loading.tsx
│   ├── error.tsx
│   ├── dashboard/page.tsx
│   ├── devices/
│   │   ├── page.tsx
│   │   └── new/page.tsx
│   └── merchants/page.tsx
├── (merchant)/                     # merchant area
│   ├── layout.tsx
│   ├── loading.tsx
│   ├── error.tsx
│   ├── dashboard/page.tsx
│   ├── devices/
│   │   ├── page.tsx
│   │   ├── claim/page.tsx
│   │   └── [id]/
│   │       ├── page.tsx            # configure destinations / publish
│   │       └── settings/page.tsx   # transfer, unpublish, danger actions
│   └── analytics/page.tsx
├── s/
│   └── [slug]/                     # public scan resolution
│       ├── page.tsx                # single-link: record + redirect() server-side
│       │                           # multi-link: TanStack prefetch + landing page
│       └── loading.tsx
├── api/
│   └── auth/[...all]/route.ts      # Better Auth route handler (translation only)
├── layout.tsx
├── providers.tsx
├── error.tsx
└── not-found.tsx

components/                         # shared UI: shadcn/ui (design system),
│                                   # Base UI advanced primitives, Phantom UI layout
├── common/
├── ui/                             # shadcn-generated components
├── layout/                         # Phantom UI page structure (header, nav, shell)
└── forms/                          # TanStack Form field components

db/                                 # Drizzle ORM
├── schema/                         # one module per domain (auth, merchant, device,
│                                   # destination, scan, analytics)
├── relations.ts
├── index.ts
└── seed.ts

drizzle/                            # drizzle-kit generated migrations (sql + meta)
domains/
├── auth/                           # Better Auth config, RBAC roles
├── merchant/                       # merchant accounts, admin merchant view
├── device/                         # identity, claim codes, lifecycle (publish/disable/transfer)
├── destination/                    # single/multi-link config, Google review via Places
├── scan/                           # resolution: redirect + landing, scan event recording
└── analytics/                      # merchant metrics aggregation

Each domain follows the architecture template:
domains/<name>/
├── api/
│   ├── client.ts
│   ├── queries.ts
│   └── mutations.ts
├── server/
│   ├── service.ts
│   ├── repository.ts
│   ├── mapper.ts
│   └── permissions.ts
├── components/
├── schemas/                        # Zod schemas
├── types.ts
├── constants.ts
├── utils.ts
├── index.ts
└── __tests__/

providers/                          # QueryClient provider, Better Auth client provider
hooks/                              # shared hooks (e.g. auth session)
lib/                                # pino logger, query-client, auth server singleton
scripts/                            # deploy.sh (SSH/pm2), db helpers
tests/                              # e2e (Playwright) + MSW handlers + fixtures
.github/workflows/                  # ci.yml (push/PR), deploy.yml (optional manual)
package.json
next.config.ts
tsconfig.json
drizzle.config.ts
biome.json
.env.example
```

**Structure Decision**: Single-project web application following the canonical
`NFC_PLATFORM_ARCHITECTURE_v3.md` directory tree verbatim (`app/`, `domains/`,
`components/`, `db/`, `lib/`, `providers/`, `hooks/`, `tests/`, `drizzle/`, `docs/`,
`scripts/`, `.github/`). The domain template governs every capability; six domains
(auth, merchant, device, destination, scan, analytics) map one-to-one to the spec's
business capabilities. Public scan resolution lives in `app/s/[slug]`, which is the
only public-facing route the customer device encodes.